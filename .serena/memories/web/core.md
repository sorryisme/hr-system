`src/routes` uses TanStack Router file-based routing (`__root.tsx`, `index.tsx`); `src/routeTree.gen.ts` is auto-generated by `@tanstack/router-plugin` (`autoCodeSplitting: true`) — never hand-edit it (also stated in CLAUDE.md).

`src/components/ui` currently has: button, input, label, table, dialog, dropdown-menu (shadcn `base-nova` style). `form` is not installable from the current shadcn registry version — see `mem:tech_stack` gap.

`src/components/shared` and `src/features` are empty scaffolds (`.gitkeep` only) despite planning docs referencing DataTable/FormField/PageLayout/ConfirmDialog — these need to be built from scratch, not assumed to exist.

No API client exists yet; apps/web makes no backend calls today. Once one is needed, it must be generated via Orval from the apps/api Swagger spec (CLAUDE.md: only Orval-generated types allowed, never hand-write API types).